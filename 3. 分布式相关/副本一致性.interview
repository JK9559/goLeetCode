1. 强一致性：
任何时刻任何用户都可以读到最近的一次更新的副本数据。
2. 单调一致性：
任何时刻，任何用户一旦读到某个数据在某次更新后的新的值，
那么这个用户将不会读到比此值更旧的值。
3. 会话一致性：
任何用户在某次会话内一旦读取到某个数据在某次更新后的值，
这个用户在这次会话中将不会读到比这个值旧的值。
4. 最终一致性：
一旦更新成功，各个副本上的数据最终将到达完全一致的状态，
但是到达完全一致的状态所需要的时间不能保证。
5. 弱一致性：
一旦更新成功，用户无法在一个确定的时间读到这次更新的值，
即使在某个副本读取到了新值，也不能保证在别的副本也读取到。


